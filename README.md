# YTK Partner ID Search

Adds a unique `Partner ID` field on `res.partner`, places it after `Website` on the contact form, and makes partner lookup match that field in both the contact search view and relational partner selectors.
